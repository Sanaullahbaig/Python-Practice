# Syntex
# lambda arguments : expressions
# can be used as a normal dunction

# def square(n):
#     return n*n
# print(square(5))

# We can also do it like this
square = lambda x : x*x
print(square(5))

sum = lambda a,b,c : a+b+c
print(sum(1,2,3))